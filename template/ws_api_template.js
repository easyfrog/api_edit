/**
 * WebSocket 接口的处理方法
 * @param {{
 * [_:string]: string
 * api?: string
 * refresh?: boolean
 * }} data websocket中收到的数据
 * @param {object} context 全局对象
 */
async function handler(data, context) {
    return new Promise((resolve, reject) => {

        

    })
}