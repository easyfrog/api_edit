/**
 * REST API 接口的处理方法
 * @param {import("../api/types").Options} context 
 */
async function handler(context) {
    let { api_cache, mongo, http, https, request, response } = context

    return new Promise((resolve, reject) => {

        

    })
}